apiVersion: 1

deleteDatasources:
  - name: Postgres
    type: postgres

datasources:
  - name: Postgres
    uid: ibsys-postgres
    type: postgres
    url: postgres:5432
    database: sensor_db
    user: sensor
    secureJsonData:
      password: sensorpw
    jsonData:
      sslmode: disable
      timescaledb: false
